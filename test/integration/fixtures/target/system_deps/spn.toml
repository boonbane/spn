[package]
name = "top_level_system_deps"
version = "0.1.0"
system_deps = [
  { lib = "m", when = { os = { not = "windows" } } },
  { lib = "user32", when = { os = "windows" } },
]

[[bin]]
name = "main"
source = ["main.c"]
