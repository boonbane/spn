[package]
name = "spum"
version = "1.0.0"
system_deps = [
  { lib = "m", when = { os = { not = "windows" } } },
  { lib = "user32", when = { os = "windows" } },
]

[[lib]]
name = "spum"
kinds = ["static"]
source = ["spum.c"]
headers = ["spum.h"]
